export default {
  /**
   * App Name
   *
   * Name of your app. Accessible via App.name() method.
   * And is by default the suffix of your page title.
   */
  name: 'RSuite App',
  /**
   * Default Locale
   *
   */
  locale: 'zh'
}