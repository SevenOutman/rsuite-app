module.exports = {
  /**
   * title() 函数如何格式化页面标题
   * 可用的占位符：
   *  - %s title 的参数
   *  - {APP_NAME} app_name()
   */
  titleTemplate: '%s - {APP_NAME}',
  /**
   * 当 Page 不指定 layout 时默认使用的 layout
   * 初始为 'default'
   */
  defaultLayout: 'frame',
};
